# API Standards

## REST

GET /api/projects POST /api/business-ideas GET /api/reports/:id

## Rules

-   JSON only
-   Typed responses
-   Validation using Zod
-   Consistent error format
-   Auth middleware
-   Rate limiting
-   Logging
